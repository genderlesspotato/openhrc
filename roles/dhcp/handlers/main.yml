---
# OpenBSD's dhcpd does not support reloading its configuration via SIGHUP --
# a full restart is required for changes to take effect.
- name: Restart dhcpd
  ansible.builtin.service:
    name: dhcpd
    args: "{{ network_interfaces.lan.iface }}"
    state: restarted
